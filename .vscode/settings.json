{
  "files.exclude": {
    "**/*.jsaa": {
      "when": "$(basename).ts"
    },
    "**/**.jsaa": {
      "when": "$(basename).tsx"
    }
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "eslint.rules.customizations": [
    { "rule": "*", "severity": "warn" }
  ]
}